# Lana — SEO Guru Portfolio

> Your Everyday Digital Marketer

## 🚀 Deploy to Vercel (3 Steps)

### Option A — Vercel CLI (Fastest)
```bash
npm install -g vercel
npm install
vercel
```
Follow the prompts — it auto-detects Vite and deploys in ~60 seconds.

### Option B — GitHub + Vercel Dashboard
1. Push this folder to a GitHub repo
2. Go to [vercel.com](https://vercel.com) → **New Project**
3. Import your GitHub repo
4. Vercel auto-detects Vite settings — click **Deploy**

### Option C — Drag & Drop
1. Run `npm install && npm run build` locally
2. Drag the `dist/` folder to [vercel.com/new](https://vercel.com/new)

---

## 🛠 Local Development
```bash
npm install
npm run dev
# Opens at http://localhost:5173
```

## 📦 Build for Production
```bash
npm run build
# Output in /dist folder
```

## 🏗 Project Structure
```
lana-portfolio/
├── index.html          # Entry HTML with SEO meta tags
├── vercel.json         # Vercel SPA routing config
├── vite.config.js      # Vite build config
├── package.json
└── src/
    ├── main.jsx        # React DOM entry
    └── App.jsx         # Full portfolio component
```

## ✨ Features
- Custom animated cursor
- Glassmorphism navigation
- Hero with typewriter effect & parallax
- Real photo integration
- Animated skill progress bars
- Interactive experience tabs
- Contact form with validation
- Scroll progress indicator
- Fully responsive (mobile-first)
- SEO meta tags & Open Graph

---
Built for **Oluwalanami Emmanuel (Lana)** — SEO Guru
